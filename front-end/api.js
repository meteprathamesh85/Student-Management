const API_BASE = "http://localhost:3000";

export const API = {
    login: `${API_BASE}/api/login`,
    register: `${API_BASE}/api/register`,
    homeStats: `${API_BASE}/api/home-stats`,
    students: `${API_BASE}/api/students`,
    support: `${API_BASE}/api/support`
};
